/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "i1.prth.gr",
      "i.auto-bild.de",
      "images.pexels.com",
      "res.cloudinary.com",
    ], //Domain of image host
  },
};

export default nextConfig;
